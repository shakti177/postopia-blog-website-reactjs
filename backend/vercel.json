{
    "version": 2,
    "builds": [
      {
        "src": "app.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/app.js"
      }
    ],
    "env": {
      "MONGODB_URI": "@mongodb_uri",
      "REFRESH_JWT_KEY": "@refresh_jwt_key",
      "ACCESS_JWT_KEY": "@access_jwt_key"
    }
  }